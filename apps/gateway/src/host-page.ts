import fs from "node:fs/promises"
import path from "node:path"
import { fileURLToPath } from "node:url"
import type { HostControlSnapshot } from "./host-control.js"

type HostPageAssets = {
  lockupDark: string
  lockupLight: string
  logomarkDark: string
  logomarkLight: string
}

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const brandDir = path.resolve(__dirname, "..", "..", "..", "apps", "mobile", "assets", "brand")

let assetPromise: Promise<HostPageAssets> | null = null

function escapeHtml(value: string) {
  return value
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#39;")
}

async function readPngDataUrl(fileName: string) {
  const filePath = path.join(brandDir, fileName)
  const bytes = await fs.readFile(filePath)
  return `data:image/png;base64,${bytes.toString("base64")}`
}

export function loadHostPageAssets() {
  if (!assetPromise) {
    assetPromise = Promise.all([
      readPngDataUrl("opencode-lockup-dark.png"),
      readPngDataUrl("opencode-lockup-light.png"),
      readPngDataUrl("opencode-logomark-dark.png"),
      readPngDataUrl("opencode-logomark-light.png"),
    ]).then(([lockupDark, lockupLight, logomarkDark, logomarkLight]) => ({
      lockupDark,
      lockupLight,
      logomarkDark,
      logomarkLight,
    }))
  }
  return assetPromise
}

export function renderHostPage(
  publicBaseUrl: string,
  assets: HostPageAssets,
  hostControls: HostControlSnapshot,
  pairedDeviceCount: number,
) {
  const escapedBaseUrl = escapeHtml(publicBaseUrl)
  const publicBaseUrlJson = JSON.stringify(publicBaseUrl)
  const hostControlsJson = JSON.stringify(hostControls)
  const pairedDeviceCountJson = JSON.stringify(pairedDeviceCount)

  const styles = `:root{color-scheme:dark light;--app:#080607;--canvas:#0f0c0d;--surface:#151112;--surface-elevated:#1b1718;--panel:#191516;--border:#2c2728;--border-strong:#474041;--text:#f1ecec;--text-muted:#b7b1b1;--text-dim:#7f7879;--accent:#f1ecec;--accent-strong:#fff;--accent-ink:#0e0b0b;--chip:rgba(241,236,236,.08);--chip-border:rgba(241,236,236,.14);--notice:rgba(153,183,216,.14);--notice-text:#99b7d8;--danger:#e08888;--qr-surface:#f9f7f4;--shadow:0 20px 60px rgba(0,0,0,.28)}@media (prefers-color-scheme:light){:root{--app:#f4efe9;--canvas:#fffcf8;--surface:#fcf8f3;--surface-elevated:#fff;--panel:#ebe4dc;--border:#d9d0c6;--border-strong:#b9ac9d;--text:#171311;--text-muted:#615851;--text-dim:#867a70;--accent:#171311;--accent-strong:#2e2723;--accent-ink:#f7f3ee;--chip:rgba(23,19,17,.04);--chip-border:rgba(23,19,17,.08);--notice:rgba(45,97,143,.08);--notice-text:#2d618f;--danger:#aa3f43;--qr-surface:#fff;--shadow:0 18px 40px rgba(23,19,17,.08)}}*{box-sizing:border-box}[hidden]{display:none!important}body{margin:0;min-height:100vh;background:radial-gradient(circle at top,rgba(153,183,216,.08),transparent 34%),linear-gradient(180deg,var(--canvas),var(--app));color:var(--text);font-family:Inter,"Segoe UI",ui-sans-serif,system-ui,sans-serif}body.modal-open{overflow:hidden}.shell{width:min(1120px,calc(100vw - 32px));margin:32px auto}.hero{border:1px solid var(--border);background:color-mix(in srgb,var(--surface-elevated) 84%,transparent);box-shadow:var(--shadow);border-radius:30px;padding:28px}.hero-top{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:18px}.eyebrow{display:inline-flex;align-items:center;gap:10px;padding:9px 12px;border-radius:999px;border:1px solid var(--chip-border);background:var(--chip);color:var(--text-muted);font-size:12px;letter-spacing:.12em;text-transform:uppercase}.eyebrow .mark{width:18px;height:18px}.brand-image{display:block;width:min(420px,100%);height:auto}h1{margin:16px 0 10px;font-size:clamp(34px,5vw,52px);line-height:.95;letter-spacing:-.04em}.intro{max-width:720px;margin:0;color:var(--text-muted);font-size:18px;line-height:1.6}.chip-row{display:flex;flex-wrap:wrap;gap:10px;margin-top:20px}.chip{border:1px solid var(--chip-border);background:var(--chip);color:var(--text-muted);border-radius:999px;padding:9px 12px;font-size:13px;font-weight:600}.cta-row{display:flex;align-items:center;gap:14px;flex-wrap:wrap;margin-top:24px}button,.copy-button{appearance:none;border:none;cursor:pointer;border-radius:999px;font:inherit}.primary-button{background:var(--accent);color:var(--accent-ink);padding:14px 18px;font-weight:700;min-width:220px}.secondary-button,.danger-button,.ghost-button{padding:12px 16px;font-weight:700}.secondary-button,.danger-button{min-width:220px}.secondary-button{background:var(--chip);border:1px solid var(--chip-border);color:var(--text)}.danger-button{background:color-mix(in srgb,var(--danger) 18%,transparent);border:1px solid color-mix(in srgb,var(--danger) 42%,var(--border));color:var(--text)}.ghost-button{background:transparent;border:1px solid var(--chip-border);color:var(--text)}.primary-button[disabled],.secondary-button[disabled],.danger-button[disabled],.ghost-button[disabled]{opacity:.6;cursor:not-allowed}.status{color:var(--text-muted);font-size:14px}.layout{display:grid;grid-template-columns:minmax(320px,1.05fr) minmax(280px,.95fr);gap:20px;margin-top:28px}.panel{background:color-mix(in srgb,var(--surface) 92%,transparent);border:1px solid var(--border);border-radius:26px;padding:22px}.panel-title{margin:0 0 14px;color:var(--text-dim);font-size:12px;letter-spacing:.16em;text-transform:uppercase}.value-group{display:grid;gap:16px}.value-row{display:grid;gap:8px}.value-row strong{font-size:14px}.value-box{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;padding:12px 14px;border-radius:18px;border:1px solid var(--border);background:var(--panel)}.value-box code{flex:1;margin:0;color:var(--text);font-size:15px;line-height:1.5;word-break:break-all;white-space:pre-wrap;font-family:ui-monospace,SFMono-Regular,Menlo,monospace}.copy-button{padding:8px 12px;background:var(--chip);border:1px solid var(--chip-border);color:var(--text);font-size:13px;font-weight:700}.notice{margin-top:18px;padding:14px 16px;border-radius:18px;border:1px solid color-mix(in srgb,var(--notice-text) 25%,var(--border));background:var(--notice);color:var(--notice-text);font-size:14px;line-height:1.5}.result-meta{margin-top:14px;color:var(--text-muted);font-size:14px}.result-meta.error{color:var(--danger)}.controls-panel{grid-column:1 / -1}.control-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px}.control-card{display:grid;gap:14px;padding:18px;border-radius:22px;border:1px solid var(--border);background:var(--panel)}.control-copy{display:grid;gap:8px}.control-copy strong{font-size:16px}.control-copy p{margin:0;color:var(--text-muted);font-size:14px;line-height:1.6}.control-actions{display:flex;align-items:center;justify-content:space-between;gap:14px;flex-wrap:wrap}.status-pill{display:inline-flex;align-items:center;border-radius:999px;padding:8px 12px;border:1px solid var(--chip-border);background:var(--chip);color:var(--text-muted);font-size:13px;font-weight:700}.status-button{max-width:100%;text-align:left}.status-pill.on{color:var(--accent-strong)}.status-pill.off{color:var(--text-muted)}.status-pill.unavailable{color:var(--text-dim)}.status-button[disabled]{cursor:not-allowed;opacity:.65}.qr-panel{display:flex;align-items:center;justify-content:center;min-height:360px}.qr-shell{width:100%;display:grid;place-items:center;gap:16px}.qr-card{width:min(320px,100%);aspect-ratio:1;display:grid;place-items:center;padding:18px;background:var(--qr-surface);border-radius:28px;border:1px solid var(--border)}.qr-card img{width:100%;height:auto;display:block}.qr-empty{text-align:center;color:var(--text-dim);font-size:15px;line-height:1.6}.modal-layer{position:fixed;inset:0;z-index:30;display:grid;place-items:center;padding:20px}.modal-backdrop{position:absolute;inset:0;background:rgba(8,6,7,.72);backdrop-filter:blur(10px)}.modal-card{position:relative;width:min(760px,100%);max-height:min(82vh,920px);overflow:auto;border-radius:28px;border:1px solid var(--border);background:color-mix(in srgb,var(--surface-elevated) 92%,transparent);box-shadow:var(--shadow);padding:24px;display:grid;gap:18px}.modal-head{display:flex;align-items:flex-start;justify-content:space-between;gap:16px}.modal-title{margin:4px 0 0;font-size:clamp(28px,4vw,36px);line-height:1;letter-spacing:-.03em}.modal-copy,.modal-empty,.modal-loading{color:var(--text-muted);font-size:15px;line-height:1.7}.modal-copy{display:grid;gap:10px}.modal-copy p{margin:0}.modal-actions{display:flex;justify-content:flex-end;gap:12px;flex-wrap:wrap}.device-list{display:grid;gap:14px}.device-card{display:grid;gap:14px;padding:18px;border-radius:22px;border:1px solid var(--border);background:var(--panel)}.device-card-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;flex-wrap:wrap}.device-card-title{display:grid;gap:6px}.device-card-title strong{font-size:18px}.device-card-subtitle,.device-card-support{margin:0}.device-card-subtitle{color:var(--text-muted);font-size:15px}.device-card-support{color:var(--text-dim);font-size:13px}.device-meta-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.device-meta{padding:12px 14px;border-radius:16px;border:1px solid var(--border);background:color-mix(in srgb,var(--surface) 70%,transparent)}.device-meta-label{display:block;margin-bottom:8px;color:var(--text-dim);font-size:11px;letter-spacing:.14em;text-transform:uppercase}.device-meta-value{color:var(--text);font-size:14px;line-height:1.6}footer{margin-top:18px;color:var(--text-dim);font-size:13px;line-height:1.6}@media (max-width:860px){.shell{width:min(100vw - 20px,100%);margin:16px auto 28px}.hero{border-radius:24px;padding:18px}.hero-top{flex-direction:column;align-items:flex-start}.layout,.control-grid,.device-meta-grid{grid-template-columns:1fr}.qr-panel{min-height:0}.primary-button,.secondary-button,.danger-button,.ghost-button{width:100%}.control-actions,.modal-actions{align-items:stretch}.modal-layer{padding:12px}.modal-card{padding:18px}}`
  const markup = `<main class="shell"><section class="hero"><div class="hero-top"><span class="eyebrow"><picture><source media="(prefers-color-scheme: light)" srcset="${assets.logomarkLight}" /><img class="mark" src="${assets.logomarkDark}" alt="" /></picture>OpenCode companion</span></div><picture><source media="(prefers-color-scheme: light)" srcset="${assets.lockupLight}" /><img class="brand-image" src="${assets.lockupDark}" alt="OpenCode" /></picture><h1>Pair this host in one scan.</h1><p class="intro">OpenCode Remote keeps your local OpenCode server on this PC, then exposes a short-lived pairing challenge for your phone. Your phone and this PC must be on the same Wi-Fi network.</p><div class="chip-row"><span class="chip">LAN-first pairing</span><span class="chip">Use ${escapedBaseUrl} in the app</span><span class="chip">Short-lived QR challenge</span></div><div class="cta-row"><button id="pair" class="primary-button">Generate pairing challenge</button><span id="status" class="status">Ready to generate a new pairing challenge.</span></div><section class="layout"><article class="panel"><p class="panel-title">Host details</p><div class="value-group"><div class="value-row"><strong>Server URL</strong><div class="value-box"><code id="server-url">${escapedBaseUrl}</code><button type="button" class="copy-button" data-copy-target="server-url">Copy</button></div></div><div class="value-row"><strong>Challenge ID</strong><div class="value-box"><code id="challenge-id">Generate a pairing challenge to reveal the host handshake.</code><button type="button" class="copy-button" data-copy-target="challenge-id">Copy</button></div></div><div class="value-row"><strong>Code</strong><div class="value-box"><code id="code-value">Generate a pairing challenge to reveal the temporary code.</code><button type="button" class="copy-button" data-copy-target="setup-values">Copy all</button></div></div><div class="value-row"><strong>Expires</strong><div class="value-box"><code id="expires-at">Waiting for a fresh challenge.</code></div></div></div><div class="notice">Open the mobile app, scan the QR code, or paste the Server URL, Challenge ID, and Code manually.</div><div id="error" class="result-meta" aria-live="polite"></div></article><aside class="panel qr-panel"><div class="qr-shell"><p class="panel-title">Pairing QR</p><div class="qr-card" id="qr-card"><p id="qr-empty" class="qr-empty">Generate a pairing challenge and scan it from your phone.</p><img id="qr-image" alt="Pairing QR code" hidden /></div><p class="result-meta">Keep this page open while you pair.</p></div></aside><article class="panel controls-panel"><p class="panel-title">Host controls</p><div class="control-grid"><section class="control-card"><div class="control-copy"><strong>Automatic startup</strong><p id="startup-note">Keep OpenCode Remote available after you turn on this PC and sign in to Windows.</p></div><div class="control-actions"><span id="startup-state" class="status-pill">Checking...</span><button id="startup-toggle" type="button" class="secondary-button">Turn on automatic startup</button></div></section><section class="control-card"><div class="control-copy"><strong>Linked phones</strong><p id="device-note">Unlink every paired phone from this PC. Phones will need to pair again before they can reconnect.</p></div><div class="control-actions"><button id="device-state" type="button" class="status-pill status-button off" aria-haspopup="dialog" aria-controls="linked-devices-modal">Checking linked phones...</button><button id="unlink-devices" type="button" class="danger-button">Unlink all paired phones</button></div></section><section class="control-card"><div class="control-copy"><strong>Stop OpenCode on this PC</strong><p>Turn off the OpenCode server and gateway from here when you want the machine free for games or anything else.</p></div><div class="control-actions"><span class="status-pill off">Stops the host stack</span><button id="disconnect-host" type="button" class="danger-button">Stop everything now</button></div></section></div><div id="control-feedback" class="result-meta" aria-live="polite"></div></article></section></section><footer>OpenCode Remote is an independent companion app and is not affiliated with or endorsed by the OpenCode maintainers.</footer></main><div id="linked-devices-modal" class="modal-layer" hidden><div class="modal-backdrop" id="linked-devices-backdrop"></div><section class="modal-card" role="dialog" aria-modal="true" aria-labelledby="linked-devices-title"><div class="modal-head"><div><p class="panel-title">Linked phones</p><h2 id="linked-devices-title" class="modal-title">Linked phones on this PC</h2></div><button id="close-linked-devices" type="button" class="ghost-button">Close</button></div><div id="linked-devices-intro" class="modal-copy"><p>Anyone on this Wi-Fi network who can open this page can inspect linked phone details after you reveal them.</p><p>Reveal the list only when you want to check paired devices, last seen times, and approximate city or country for this host.</p></div><div id="linked-devices-loading" class="modal-loading" hidden>Loading linked phone details...</div><div id="linked-devices-error" class="result-meta error" hidden></div><div id="linked-devices-empty" class="modal-empty" hidden>No phones are linked to this PC right now.</div><div id="linked-devices-list" class="device-list" hidden></div><div class="modal-actions"><button id="reveal-linked-devices" type="button" class="secondary-button">Reveal linked phone details</button><button id="dismiss-linked-devices" type="button" class="ghost-button">Close</button></div></section></div>`
  const script = `const publicBaseUrl=${publicBaseUrlJson};const hostControls=${hostControlsJson};let pairedDeviceCount=${pairedDeviceCountJson};const pairButton=document.getElementById("pair");const status=document.getElementById("status");const error=document.getElementById("error");const challengeId=document.getElementById("challenge-id");const codeValue=document.getElementById("code-value");const expiresAt=document.getElementById("expires-at");const qrImage=document.getElementById("qr-image");const qrEmpty=document.getElementById("qr-empty");const startupState=document.getElementById("startup-state");const startupNote=document.getElementById("startup-note");const startupToggle=document.getElementById("startup-toggle");const deviceState=document.getElementById("device-state");const deviceNote=document.getElementById("device-note");const unlinkDevices=document.getElementById("unlink-devices");const disconnectHost=document.getElementById("disconnect-host");const controlFeedback=document.getElementById("control-feedback");const linkedDevicesModal=document.getElementById("linked-devices-modal");const linkedDevicesBackdrop=document.getElementById("linked-devices-backdrop");const closeLinkedDevices=document.getElementById("close-linked-devices");const dismissLinkedDevices=document.getElementById("dismiss-linked-devices");const revealLinkedDevices=document.getElementById("reveal-linked-devices");const linkedDevicesIntro=document.getElementById("linked-devices-intro");const linkedDevicesLoading=document.getElementById("linked-devices-loading");const linkedDevicesError=document.getElementById("linked-devices-error");const linkedDevicesEmpty=document.getElementById("linked-devices-empty");const linkedDevicesList=document.getElementById("linked-devices-list");let startupBusy=false;let unlinkBusy=false;let disconnectBusy=false;let linkedDevicesBusy=false;let linkedDevicesOpen=false;let linkedDevicesRevealed=false;let linkedDevicesLoaded=false;let linkedDevices=[];let linkedDevicesErrorText="";let linkedDevicesLastFocus=null;function setError(message){error.textContent=message||"";error.classList.toggle("error",Boolean(message))}function setControlFeedback(message,isError=false){controlFeedback.textContent=message||"";controlFeedback.classList.toggle("error",Boolean(isError&&message))}function formatDateTime(value){const date=new Date(value);if(Number.isNaN(date.getTime())){return"Unknown"}return date.toLocaleString(undefined,{dateStyle:"medium",timeStyle:"short"})}function formatPlatformLabel(platform){switch(platform){case"ios":return"iOS";case"android":return"Android";case"web":return"Web";default:return"Unknown"}}function formatModelLabel(device){return device.modelName||"Model not shared"}function formatOsLabel(device){const parts=[device.osName,device.osVersion].filter(Boolean);return parts.length>0?parts.join(" "):"OS details unavailable"}function formatLocationLabel(device){const parts=[device.locationCity,device.locationCountry].filter(Boolean);return parts.length>0?parts.join(", "):"Location not shared"}function createElement(tagName,className,textContent){const element=document.createElement(tagName);if(className){element.className=className}if(textContent!=null){element.textContent=textContent}return element}function createDeviceMeta(label,value){const wrapper=createElement("div","device-meta");wrapper.append(createElement("span","device-meta-label",label),createElement("span","device-meta-value",value));return wrapper}function renderLinkedDevicesList(){if(!linkedDevicesList){return}const fragment=document.createDocumentFragment();linkedDevices.forEach((device)=>{const card=createElement("article","device-card");const head=createElement("div","device-card-head");const titleGroup=createElement("div","device-card-title");titleGroup.append(createElement("strong","",device.name),createElement("p","device-card-subtitle",formatModelLabel(device)),createElement("p","device-card-support",formatOsLabel(device)));head.append(titleGroup,createElement("span","status-pill on",formatPlatformLabel(device.platform)));const metaGrid=createElement("div","device-meta-grid");metaGrid.append(createDeviceMeta("Paired",formatDateTime(device.createdAt)),createDeviceMeta("Last seen",formatDateTime(device.lastSeenAt)),createDeviceMeta("Approximate location",formatLocationLabel(device)));card.append(head,metaGrid);fragment.append(card)});linkedDevicesList.replaceChildren(fragment)}function applyLinkedDevicesModalState(){if(!linkedDevicesModal){return}linkedDevicesModal.hidden=!linkedDevicesOpen;document.body.classList.toggle("modal-open",linkedDevicesOpen);if(!linkedDevicesOpen){return}const shouldShowReveal=(!linkedDevicesRevealed||Boolean(linkedDevicesErrorText))&&pairedDeviceCount>0;const showList=linkedDevicesRevealed&&!linkedDevicesBusy&&!linkedDevicesErrorText&&linkedDevicesLoaded&&linkedDevices.length>0;const showEmpty=linkedDevicesRevealed&&!linkedDevicesBusy&&!linkedDevicesErrorText&&linkedDevicesLoaded&&linkedDevices.length===0;linkedDevicesIntro.hidden=linkedDevicesRevealed;linkedDevicesLoading.hidden=!(linkedDevicesBusy&&linkedDevicesRevealed);linkedDevicesError.hidden=!linkedDevicesErrorText;linkedDevicesError.textContent=linkedDevicesErrorText;linkedDevicesEmpty.hidden=!showEmpty;linkedDevicesList.hidden=!showList;revealLinkedDevices.hidden=!shouldShowReveal;revealLinkedDevices.disabled=linkedDevicesBusy||pairedDeviceCount<=0;revealLinkedDevices.textContent=linkedDevicesErrorText?"Try again":"Reveal linked phone details"}function openLinkedDevicesModal(){if(!deviceState||deviceState.disabled){return}linkedDevicesLastFocus=document.activeElement instanceof HTMLElement?document.activeElement:null;linkedDevicesOpen=true;applyLinkedDevicesModalState();window.requestAnimationFrame(()=>{if(!linkedDevicesRevealed&&!revealLinkedDevices.hidden){revealLinkedDevices.focus();return}closeLinkedDevices.focus()})}function closeLinkedDevicesModal(){if(!linkedDevicesOpen){return}linkedDevicesOpen=false;applyLinkedDevicesModalState();if(linkedDevicesLastFocus&&typeof linkedDevicesLastFocus.focus==="function"){linkedDevicesLastFocus.focus()}}async function loadLinkedDevices(){linkedDevicesBusy=true;linkedDevicesRevealed=true;linkedDevicesErrorText="";applyLinkedDevicesModalState();try{const response=await fetch("/host/devices",{method:"GET"});const data=await readJsonSafe(response);if(!response.ok){throw new Error(data.error||"Failed to load linked phone details.")}linkedDevices=Array.isArray(data)?data:[];linkedDevicesLoaded=true;renderLinkedDevicesList()}catch(requestError){linkedDevicesLoaded=false;linkedDevices=[];linkedDevicesErrorText=requestError instanceof Error?requestError.message:"Failed to load linked phone details."}finally{linkedDevicesBusy=false;applyLinkedDevicesModalState()}}function applyHostControls(){const startupSupported=Boolean(hostControls.startupSupported);const startupEnabled=Boolean(hostControls.startupEnabled);const disconnectSupported=Boolean(hostControls.disconnectSupported);const hasPairedDevices=pairedDeviceCount>0;startupState.textContent=startupSupported?startupEnabled?"Automatic startup is on":"Automatic startup is off":"Automatic startup unavailable";startupState.className="status-pill "+(startupSupported?startupEnabled?"on":"off":"unavailable");startupNote.textContent=startupSupported?startupEnabled?"This PC will restart OpenCode Remote automatically after you sign in to Windows.":"OpenCode Remote stays off until you start it manually.":"Automatic startup controls are unavailable on this host.";startupToggle.textContent=startupEnabled?"Turn off automatic startup":"Turn on automatic startup";startupToggle.disabled=!startupSupported||startupBusy||unlinkBusy||disconnectBusy;deviceState.textContent=hasPairedDevices?pairedDeviceCount===1?"1 phone linked":pairedDeviceCount+" phones linked":"No phones linked";deviceState.className="status-pill status-button "+(hasPairedDevices?"on":"off");deviceState.disabled=!hasPairedDevices||unlinkBusy||disconnectBusy;deviceNote.textContent=hasPairedDevices?"Open the linked-phone count to inspect paired devices, then use unlink when you want every phone to pair again.":"There are no paired phones linked to this PC right now.";unlinkDevices.disabled=!hasPairedDevices||unlinkBusy||disconnectBusy;disconnectHost.disabled=!disconnectSupported||disconnectBusy;if(linkedDevicesOpen){applyLinkedDevicesModalState()}}async function readJsonSafe(response){try{return await response.json()}catch(_error){return {}}}async function copyById(targetId){const text=targetId==="setup-values"?["Server URL: "+publicBaseUrl,"Challenge ID: "+challengeId.textContent,"Code: "+codeValue.textContent,"Expires: "+expiresAt.textContent].join("\\n"):document.getElementById(targetId)?.textContent||"";if(!text)return;try{await navigator.clipboard.writeText(text);status.textContent="Copied to clipboard."}catch(_clipboardError){setError("Clipboard access failed. Copy the values manually.")}}document.querySelectorAll("[data-copy-target]").forEach((button)=>{button.addEventListener("click",()=>copyById(button.getAttribute("data-copy-target")))});applyHostControls();deviceState.addEventListener("click",()=>{openLinkedDevicesModal()});revealLinkedDevices.addEventListener("click",async()=>{if(linkedDevicesBusy||pairedDeviceCount<=0){return}await loadLinkedDevices()});closeLinkedDevices.addEventListener("click",()=>{closeLinkedDevicesModal()});dismissLinkedDevices.addEventListener("click",()=>{closeLinkedDevicesModal()});linkedDevicesBackdrop.addEventListener("click",()=>{closeLinkedDevicesModal()});document.addEventListener("keydown",(event)=>{if(event.key==="Escape"&&linkedDevicesOpen){event.preventDefault();closeLinkedDevicesModal()}});startupToggle.addEventListener("click",async()=>{if(startupBusy||!hostControls.startupSupported){return}const nextEnabled=!hostControls.startupEnabled;startupBusy=true;applyHostControls();setControlFeedback(nextEnabled?"Turning on automatic startup...":"Turning off automatic startup...");try{const response=await fetch("/host/windows-startup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({enabled:nextEnabled})});const data=await readJsonSafe(response);if(!response.ok){throw new Error(data.error||"Failed to update automatic startup.")}hostControls.startupEnabled=Boolean(data.startupEnabled);hostControls.startupSupported=Boolean(data.startupSupported);hostControls.disconnectSupported=Boolean(data.disconnectSupported);setControlFeedback(hostControls.startupEnabled?"Automatic startup is now on.":"Automatic startup is now off.")}catch(requestError){const message=requestError instanceof Error?requestError.message:"Failed to update automatic startup.";setControlFeedback(message,true)}finally{startupBusy=false;applyHostControls()}});unlinkDevices.addEventListener("click",async()=>{if(unlinkBusy||pairedDeviceCount<=0){return}const confirmed=window.confirm("Unlink all paired phones from this PC? Every phone will need to pair again before it can reconnect.");if(!confirmed){return}unlinkBusy=true;applyHostControls();setControlFeedback("Unlinking paired phones from this PC...");try{const response=await fetch("/host/devices/unlink",{method:"POST"});const data=await readJsonSafe(response);if(!response.ok){throw new Error(data.error||"Failed to unlink paired phones from this PC.")}pairedDeviceCount=Number(data.remaining??0);linkedDevices=[];linkedDevicesLoaded=true;linkedDevicesErrorText="";renderLinkedDevicesList();setControlFeedback(Number(data.revoked??0)>0?"All paired phones have been unlinked from this PC.":"There were no paired phones left to unlink.")}catch(requestError){const message=requestError instanceof Error?requestError.message:"Failed to unlink paired phones from this PC.";unlinkBusy=false;setControlFeedback(message,true);applyHostControls();return}unlinkBusy=false;applyHostControls()});disconnectHost.addEventListener("click",async()=>{if(disconnectBusy||!hostControls.disconnectSupported){return}const confirmed=window.confirm("Stop OpenCode Remote on this PC? This will turn off the OpenCode server and gateway until you start them again.");if(!confirmed){return}disconnectBusy=true;applyHostControls();setControlFeedback("Stopping OpenCode Remote on this PC...");try{const response=await fetch("/host/disconnect",{method:"POST"});const data=await readJsonSafe(response);if(!response.ok){throw new Error(data.error||"Failed to stop OpenCode Remote on this PC.")}status.textContent="Host shutdown requested.";setControlFeedback("OpenCode Remote is shutting down on this PC. This page will stop responding in a moment.")}catch(requestError){const message=requestError instanceof Error?requestError.message:"Failed to stop OpenCode Remote on this PC.";disconnectBusy=false;setControlFeedback(message,true);applyHostControls()}});pairButton.addEventListener("click",async()=>{pairButton.disabled=true;setError("");status.textContent="Generating a fresh pairing challenge...";try{const response=await fetch("/mobile/pair/start",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({})});const data=await response.json();if(!response.ok){throw new Error(data.error||"Failed to create the pairing challenge.")}challengeId.textContent=data.challengeId;codeValue.textContent=data.code;expiresAt.textContent=new Date(data.expiresAt).toLocaleString();qrImage.src=data.qrCodeDataUrl;qrImage.hidden=false;qrEmpty.hidden=true;status.textContent="Pairing challenge ready. Scan the QR code or copy the values into the mobile app."}catch(requestError){const message=requestError instanceof Error?requestError.message:"Unexpected pairing error.";setError(message);status.textContent="Pairing challenge failed."}finally{pairButton.disabled=false}});`

  return `<!doctype html><html lang="en"><head><meta charset="utf-8" /><meta name="viewport" content="width=device-width, initial-scale=1" /><title>OpenCode Remote Pairing</title><style>${styles}</style></head><body>${markup}<script>${script}</script></body></html>`
}
